name: jack_rtc_call
description: Package for WebRTC and Callkit
version: 0.0.1
homepage:

environment:
    sdk: ">=3.0.3 <4.0.0"
    flutter: ">=1.17.0"

dependencies:
    flutter:
        sdk: flutter
    flutter_callkit_incoming: ^2.0.0+1
    flutter_webrtc: ^0.9.34
    rxdart: ^0.27.7
    socket_io_client: ^2.0.2
    uuid: ^3.0.7

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
    # Uncomment this when you install new package
    # plugin:
    #     platforms:
    #         android:
    #             package: com.hiennv.flutter_callkit_incoming
    #             pluginClass: FlutterCallkitIncomingPlugin
    #         ios:
    #             pluginClass: FlutterCallkitIncomingPlugin
